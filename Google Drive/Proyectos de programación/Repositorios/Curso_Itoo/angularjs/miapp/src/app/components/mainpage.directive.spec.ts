import { MainpageDirective } from './mainpage.directive';

describe('MainpageDirective', () => {
  it('should create an instance', () => {
    const directive = new MainpageDirective();
    expect(directive).toBeTruthy();
  });
});
